# Codexタスク: 得能3種の未配線を解消し、パワー補助指標の欠落を埋める

作業ルート: `C:\Users\amila\Desktop\Claude Code\パワプロ風ペナント開発`
対象コミット: **b700ab24bd87198fb272b0541ebb6fb7f85e0f73**（この版を読むこと。報告の冒頭にこのハッシュを書く）

## 背景

2026-08-05に仕様と実装を突き合わせた監査で、次の2件が見つかった。どちらも
「仕様が使えと言っている材料を取りこぼしている」型。**あなたが直すのはこの2件だけ**。

## タスク1: 実装済みなのに本番から呼ばれていない得能3種を配線する

`src/ratings/special_abilities.mjs` に次の3関数があり、テストも通っているのに、
カード生成（`src/cards/pipeline.mjs`）から**一度も呼ばれていない**。

| 関数 | 場所 | 仕様 |
|---|---|---|
| `strikeoutAbility` | special_abilities.mjs:31 | 仕様05 §5（三振の赤特／選球眼の青特） |
| `infieldHitAbility` | special_abilities.mjs:43 | 仕様05 §7（内野安打○） |
| `goldSpecialAbilities` | special_abilities.mjs:61 | 仕様05 §9（金特・歴代上位0.5%のみ） |

証拠: `grep -rn "goldSpecialAbilities\|strikeoutAbility" src/cards/` の結果が空。
`infieldHitAbility` は pipeline.mjs:361 で **`null` が渡されている**（呼ばずにnull固定）。

### やること

1. 3つをカード生成の経路へ繋ぎ、結果を**カードの得能欄に出す**
   （`src/cards/ability_sheet.mjs` の `abilities` に項目を足す形。既存の項目の作りに合わせる）
2. `goldSpecialAbilities` は歴代分布（`historical`）が要る。
   `{[指標キー]: {p995, p999, max}}` の形で、**2006-2025の全打者から実データで作る**こと。
   対象キーは `configs/ratings.json` の `special_abilities.gold.criteria`（`hrPer500` と `avgEnv`）。
   作った分布は `outputs/derived/` へ保存し、再生成できるようスクリプト化する
3. `infieldHitAbility` の引数 `gbSingleExcess`（ゴロ単打の超過）を実データから作る。
   材料の在り処は自分で調べること（ヒント: `nf3_team_bat.ih` が内野安打、
   `src/ratings/running.mjs` が既に内野安打率を使っている）

### 守ること

- **金特は歴史的水準のみ**。MVP・タイトル・知名度では付けない（仕様05 §9、回帰テストQ12が既に縛っている）
- 該当しない選手には**空配列でなくその項目を出さない**（「該当なし」と「未査定」を混同しない）
- 既存の回帰テストを壊さない。特に `scripts/test_ledger_regressions.mjs` の Q3/Q3-b（三振の二重減点なし）

## タスク2: パワーの補助指標が仕様11個中2個しかない

仕様 `docs/satei_handoff/02_CURRENT_SPEC_V2.md` §6.4 が「本塁打だけで決めない」として
複数の補助指標を挙げているのに、実装（`src/ratings/from_rates.mjs` の `blendedHrRate`）は
**ISOと二/三塁打しか使っていない**。しかもISOの効き幅は30本打者で約3%と小さい。

### やること

1. **まず仕様を全文読み、§6.4が実際に何を挙げているかを逐語で列挙する**
   （「11個」というのは監査時の概数。正確な数と中身はあなたが原典で確認すること）
2. 各指標について、**このプロジェクトのDBで実際に取れるか**を列名レベルで確認する
   （`data/pennant.db`。取れないものは「取れない」と根拠つきで報告）
3. 取れるものについて、**現行のパワー査定に足すと精度が上がるか**を実データで測る

### ★重要な制約（違反すると成果が使えない）

- **パワプロの実装値を「採否の物差し」にしてはいけない**（2026-07-31オーナー確定）。
  材料選定の検出器としては使ってよいが、「パワプロとの相関が上がったから採用」は禁止。
  2026-08-04に一度これをやって撤回している。**採否はオーナー裁定**なので、
  あなたは「測った結果」を出すところまで。採用・不採用を決めない
- **係数のハードコード禁止**。新しい係数は `configs/ratings.json` へ分離し、根拠を記録する
- **欠損を0で埋めない**（null / 0 / 推定+フラグを区別）
- **同じ情報を能力と特殊能力に二重計上しない**。ISOと新指標が同じことを言っていないか
  実データで相関を測って確認すること

## 出力

1. `_codex_result_20260805_abilities.md` に報告を書く（**冒頭に対象コミットハッシュ**）
2. 実装は直接コミットせず、変更したファイルを報告に列挙する
3. **項目ごとに結果を追記保存する**（途中で止まっても進捗が残るように）
4. 各主張には**根拠を file:line か実測値で添える**。「あるはず」で書かない
5. 日本語で書く

## 判断できない点があったら

無理に決めず「**オーナー判断が要る**」として論点を書く。特に上の「採否の物差し」に触れる判断は
必ずそうすること。
