name: Audit joint speed temporal windows
on:
  push:
    branches: [agent/satei-phase1-safety]
    paths: [.github/workflows/audit-joint-speed-temporal-windows.yml]
permissions:
  contents: read
jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/satei-phase1-safety
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node --check scripts/audit_joint_speed_temporal_windows.mjs
      - run: node scripts/audit_joint_speed_temporal_windows.mjs
