# Speed project handoff — 2026-08-16 16:50 JST

Repository: `L-carp55/pawapuro-pennant-gpt-handoff`

This file is intentionally short. Use `docs/state/speed_task_registry.tsv` as the authoritative task/status source rather than copying a stale full blocker list here.

## 1. Last verified completed wave

Branch: `review/opus-speed-owner-blockers-wave-20260816`

Verified remote HEAD: `4022cc54a5ad6ea0869468a6c3c6a3360cb13337`

Browser GPT independently checked the branch HEAD, artifacts, QA and registry before this handoff.

Closed in that wave:
- `SP-036 = DONE_NEGATIVE_FINDING`
- `SP-022 = DONE_VALIDATED`
- `SP-043 = DONE_VALIDATED`
- `SP-074 = DONE_NEGATIVE_FINDING`
- `SP-075 = DONE_VALIDATED`

Key final SP-075 facts:
- 100/100 players joined
- active Community owner-review context = 18 players, superseding the stale 55-player aggregate
- automatic rating changes = 0
- automatic stale promotions = 0
- Prospi contributes zero directional consensus and zero stale support
- independent QA = PASS, 17,283 checks, 0 failed

Important artifact:
- `outputs/derived/sp075_stale_conflict_rediagnosis_v4_20260816.json`
- `outputs/derived/opus_speed_owner_blockers_wave_20260816_qa.json`
- `docs/audits/opus_speed_owner_blockers_wave_20260816.md`

## 2. Community / SNS conclusions that must not be reopened casually

### X

The old headline `138 X speed-relevant / 55 current-100` is superseded for active appraisal use.

Owner-filtered current X layer:
- current PowerPro rating claims = 19
- directional PowerPro claims = 11
- current100 players with PowerPro evidence = 12
- current100 players with directional PowerPro evidence = 8
- current real-world speed observations = 10
- current100 players with physical observations = 6
- historical PowerPro rows archived = 41
- Prospi rows excluded = 180
- value judgment = `X_COLLECTION_LIMITED_VALUE`

Canonical cleanup branch/head:
- `review/x-powerpro-current-cleanup-20260816`
- `0ed31354b160676ba5cb246e39ceccec21e6c45e`

Policy:
- no more X collection unless owner explicitly reopens it
- Prospi-related X game/rating/gameplay posts are excluded from active appraisal use because X posts do not reliably distinguish console Prospi from Prospi A and Prospi A ratings are game-design/card-performance influenced
- pre-2025 PowerPro X posts are historical trajectory only, not current consensus
- real-world physical observations remain a separate low-weight/context lane

### YouTube

A serious process error occurred in the prior session: Browser GPT initiated a large official-YouTube collection even though the owner had said the owner would collect YouTube comments. Do not repeat this.

The owner’s intended YouTube source is NOT primarily official KONAMI videos. It is third-party videos that show/update all players’ PowerPro ratings per update, e.g. `https://www.youtube.com/watch?v=LoRwuvITSu8&t=1624s`, whose comment sections contain player-rating discussion. The owner will collect these comments. Do not start a YouTube collection without an explicit owner request.

Existing official-YouTube collection can remain archived/validated as its own bounded lane, but it should not be described as proof that third-party appraisal-video comments are low-yield.

## 3. Current active next wave

Active execution branch:
`review/opus-speed-pre-owner-review-wave-20260816`

Task-definition HEAD at time of handoff:
`cac5901597f1544fbdd23754c6033cd708e356ff`

Task file:
`docs/tasks/OPUS_SPEED_PRE_OWNER_REVIEW_WAVE_20260816.md`

Purpose:
1. repair/close `SP-016` correctly under the annual current-year-first owner policy
2. repair/close `SP-098` identity/coverage, especially 名原典彦 without inventing 2025 first-team batting data
3. inspect `SP-100` production architecture using the existing owner policy; do NOT invent owner approval for a concrete architecture if no explicit ruling exists
4. if and only if prerequisites truly close, generate `SP-077` final owner review queue and implement `SP-078` durable owner-verdict/no-overwrite machinery
5. do NOT start `SP-079`, `SP-080`, `SP-081` or shoulder

Independent work should be parallelized with subagents where useful.

No new X / YouTube / Web / NPB external collection in this wave.

## 4. Critical appraisal policies

- Annual appraisal estimates ability in that year/current point; it is not a future projection.
- Next-year repeatability/Y→Y+1 prediction must not determine annual-material selection, weights, shrinkage or final points.
- Same-time measurement reliability and sampling uncertainty are valid.
- History may be used only for explicit reasons such as genuinely insufficient current sample, injury/recovery context, obvious underperformance or temporal bridging; once current-year evidence is sufficient, automatic history pooling must be zero.
- Pure foot/baseball speed must be separated from baserunning technique and stealing skill.
- PowerPro individual player labels must never be a physical teacher, component selector, weight target, shrinkage target or player-level regression target.
- PowerPro may be used for provisional global display-scale center/width and stale/odd owner-review context.
- NPB+ direct player-screen speed measure is top/max speed only; `hp_to_1b_sec` was a misattributed-source incident and must not re-enter NPB+ reliability logic.
- Generic NPB+ reliability remains `NOT_IDENTIFIABLE` with current single-measure design; do not fabricate it.

## 5. What the next Browser GPT chat should do

First action: inspect GitHub directly. Do not trust pasted agent completion reports without checking the remote branch/head, artifacts, QA and registry.

If Opus has completed `review/opus-speed-pre-owner-review-wave-20260816`:
- verify remote HEAD
- inspect SP-016, SP-098, SP-100 output/decision, conditional SP-077/078 outputs, QA and registry
- confirm no external collection, no SP-079+, no shoulder
- then decide the next broad wave; avoid micro-cycle handoffs

If Opus has not completed:
- continue from `docs/tasks/OPUS_SPEED_PRE_OWNER_REVIEW_WAVE_20260816.md`

Use `docs/state/speed_task_registry.tsv` and `docs/state/speed_exclusion_reason_ledger.tsv` as source of truth for remaining blockers after the wave.

## 6. Process rules from owner

- Browser GPT is coordinator/integrator/final decision helper.
- Large repetitive collection should be delegated only when explicitly in scope.
- Do not expand scope on your own.
- Do not re-run expensive collection because a lane has sparse yield; a measured negative finding can be a valid endpoint.
- When an agent reports completion, Browser GPT must inspect GitHub directly.
- Critical path remains SPEED ONLY until Speed Gate closes. Do not start shoulder.
