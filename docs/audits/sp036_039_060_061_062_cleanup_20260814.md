# 既存データだけで閉じた証拠レーン（SP-036 / 039 / 020 / 060 / 061 / 062）

生成日: 2026-08-14
新規 API・広い再収集はしていない。未取得は「無い」に変換していない。

## SP-036

150件台帳の外にある generic ラベルを既存 community / YouTube 分類から掃いた。

- 台帳外 7件
- 0情報化 0件
- 独立 origin として残し、数値教師にはしない

これで EX-011 の是正（弱い方向ラベルを捨てない）は既存データ範囲で完了。

## SP-039

既存 video tie-break 17人を再利用した。広い探索は再開していない。

17人とも `VIDEO_INCONCLUSIVE`。usable full-effort 0。汚染・文脈フラグは残した。数値 T90 教師にはしない。low-weight の review レーンとして保持する。これが EX-012 の是正。

## SP-020

測定日の台帳は既存のまま。解決 15人、未解決 19人。未解決は未解決のまま残す。日付は捏造していない。公式プロフィールの追加探索はしていない（未取得であり、証拠が無いことではない）。

## SP-060

`configs/scouting.json` の走力行は 1件（鈴木誠也、owner、50m 5秒8を Prior）。100人の大半に公式スカウティング行は無い。教師にはしない。欠けている被覆は欠けとして記録した。EX-016 の棚卸し。

## SP-061

既存 Grok-X 本文に代走の言及 5件（岡大海・土田龍空・藤岡裕大・林琢真・鈴木大地）。監督判断・役割・故障が混ざる。弱い文脈であり教師ではない。EX-017。

## SP-062

既存の NPB+ top_speed / H2F / TZR-RngR は、ルート・反応・守備位置が混ざる。守備の直線追走だけを分離できるソースはリポジトリに無い。判断に使えないので閉じる。これは「探したが無かった」ではなく「分離不能」。EX-018。
